line-length = 100
target-version = "py312"

# protoc output is not ours to style, and reformatting it would make
# `generate_proto.py --check` fail against a freshly generated tree.
extend-exclude = ["app/wire/gen"]

[lint]
select = [
    "E", "W",    # pycodestyle
    "F",         # pyflakes
    "I",         # import sorting
    "B",         # bugbear
    "UP",        # pyupgrade
    "SIM",       # simplify
    "C4",        # comprehensions
    "RUF",       # ruff-specific
]
ignore = [
    "E501",      # line length is handled by the formatter, not enforced hard
    "B008",      # FastAPI Depends() in defaults is the framework idiom
]

[lint.per-file-ignores]
"tests/*" = ["B011"]
